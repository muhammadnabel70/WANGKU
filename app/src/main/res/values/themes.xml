<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Base.Theme.WANGKU" parent="Theme.Material3.Light.NoActionBar">

        <item name="colorPrimary">@color/brand_green</item>
        <item name="colorOnPrimary">@color/white</item>

        <item name="android:statusBarColor">@color/brand_green</item>
        <item name="android:windowLightStatusBar">false</item>

        <!-- This item is added to prevent content from overlapping with the status bar -->

    </style>

    <style name="Theme.WANGKU" parent="Base.Theme.WANGKU" />

    <style name="Theme.WANGKU.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />
    <style name="Theme.WANGKU.PopupOverlay" parent="ThemeOverlay.Material3.Light" />

</resources>